{
  "name": "gig-identity",
  "version": "1.0.0",
  "description": "Decentralized identity for gig workers",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./dist/output.css"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "hardhat": "^2.25.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.17"
  }
}
